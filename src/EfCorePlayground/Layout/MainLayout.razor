@inherits LayoutComponentBase

<div class="d-flex flex-column vh-100">
    <nav class="navbar navbar-dark bg-dark px-3 flex-shrink-0">
        <span class="navbar-brand mb-0 h1">
            <i class="bi bi-braces-asterisk me-2"></i>EF Core 10 Playground
        </span>
        <span class="text-light small opacity-75">
            <i class="bi bi-cpu me-1"></i>.NET WASM + PostreSQL (PGLite) + Roslyn
        </span>
    </nav>
    <main class="flex-grow-1 overflow-hidden">
        @Body
    </main>
</div>
